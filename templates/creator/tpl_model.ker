<?php

namespace application\modules\[{module_name}]\model;
defined('__APPFOLDER__') OR exit('Direct access to this file is forbidden, siya');
/*
  |-----------------------------------------------------------------------------
  | Class [{model_name}]
  |-----------------------------------------------------------------------------
  |
  | [{model_description}]
  | @author kerana,
  | @date [{model_date}],
  |
 */

class [{model_name}] extends \kerana\Ada {

    protected
    [{properties}];
    
    

    public 
            /** @array data matching attributes with table field */
            $data_[{name_min}];
    
     public function __construct()
    {
        parent::__construct();
        $this->table_name = '[{model_table}]';
        $this->table_id = '[{model_table_id}]';
        
        $this->pks = [
          [{pk_values}]  
        ];
        
        $this->data_[{name_min}] = [
            [{key_values_model}]  
        ];
    }

    
    /*
      |-------------------------------------------------------------------------
      | SELECT-METHODS
      |-------------------------------------------------------------------------
      |
     */

    
    
    
    
    
    
    /*
     |-------------------------------------------------------------------------
     | INSERT-UPDATE-METHODS
     |-------------------------------------------------------------------------
     |
    */


    /**
     * -------------------------------------------------------------------------
     * Insert new record into [{model_table}]
     * -------------------------------------------------------------------------
     * @return boolean
     */
    public function insert[{name}]($data = ''){
        
        $data_insert = (empty($data)) ? $this->data_[{name_min}] : $data;
          return $this->insert($data_insert);
        
    }
    /**
     * -------------------------------------------------------------------------
     * update record into [{model_table}]
     * -------------------------------------------------------------------------
     * @return boolean
     */
    public function update[{name}]($data = ''){
        
       $data_update = (empty($data)) ? $this->data_[{name_min}] : $data;
          return $this->update($data_update);
        
    }
    
    
    
    
 /*
  |-------------------------------------------------------------------------
  | SETTERS
  |-------------------------------------------------------------------------
  | 
 */

 [{setters}]
    
    
 
 /*
  |-------------------------------------------------------------------------
  | GETTERS
  |-------------------------------------------------------------------------
  | 
 */
 [{getters}]
}